@using Microsoft.AspNetCore.Identity

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@*@if (SignInManager.IsSignedIn(User))
    {
        <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post" id="logoutForm" class="navbar-right">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
                </li>
                <li>
                    <button type="submit" class="btn btn-link navbar-btn navbar-link">Logout</button>
                </li>
            </ul>
        </form>
    }
    else
    {
        <ul class="nav navbar-nav navbar-right">
            <li><a asp-area="Identity" asp-page="/Account/Register">Register</a></li>
            <li><a asp-area="Identity" asp-page="/Account/Login">Login</a></li>
        </ul>
    }*@

<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-brand-centered">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <ul class="nav navbar-nav menu-toggle-wrap">
                <li><a href="#" id="menu-toggle"><img src="@Url.Content("~/images/home/left_menu_toggle.png")" alt=""></a></li>
            </ul>
            <div class="navbar-brand navbar-brand-centered"><img src="@Url.Content("~/images/home/logo.png")" alt=""></div>
        </div>
        <div class="collapse navbar-collapse" id="navbar-brand-centered">
            <ul class="nav navbar-nav help-contact-nav">
                <li><a href="javascript:void(0);"><span>Help</span><i class="fa fa-question-circle" aria-hidden="true"></i></a></li>
                <li><a href="javascript:void(0);"><span>Contact us</span><i class="fa fa-phone" aria-hidden="true"></i></a></li>
            </ul>
         
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown userProfileDropdown">
                        <a class="dropdown-toggle" data-toggle="dropdown" href="javascript:void(0);">
                            <i class="fa dropdownArrow"><img src="@Url.Content("~/images/home/dropDownArrow.png")" alt=""></i>
                            <i class="fa"><img src="@Url.Content("~/images/home/my_Profile.png")" alt=""></i>
                        </a>
                        <ul class="dropdown-menu">
                            <form asp-action="logout" asp-controller="Home" id="logoutForm" class="navbar-right">
                                <li>
                                    <a asp-page="/profile" title="Manage">My Profile!</a>
                                </li>
                                <li>
                                    <button type="button" class="btn btn-link navbar-btn navbar-link">LOGOUT</button>
                                </li>
                            </form>
                        </ul>
                    </li>
                </ul>
            
        </div>
    </div>
</nav>